# leftover from the pre-nix setup script; obscura now comes from the dev shell
bin/

# node deps — installed by the `headless` dev shell, never committed
node_modules/

# run outputs + logs — regenerated per run
results/
run-logs/
*.log
